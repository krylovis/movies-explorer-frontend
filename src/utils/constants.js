export const emailPattern = '[a-z0-9._%+-]+@[a-z0-9.-]+.[a-z]{2,4}';

export const regexForEmail = /^[a-z0-9._%+-]+@[a-z0-9.-]+.[a-z]{2,4}/i;
export const regexForName = /^[a-zа-яё -]+/i;
export const regexForQuery = /^[a-zа-яё -\d]+/i;